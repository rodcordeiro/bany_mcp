DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=secret
DB_NAME=banky
DB_CONNECTION_LIMIT=10

# Optional defaults for tool calls
DEFAULT_OWNER_ID=
DEFAULT_LOOKBACK_DAYS=30

# MCP metadata
MCP_SERVER_NAME=bany-mcp
MCP_SERVER_VERSION=0.2.0
