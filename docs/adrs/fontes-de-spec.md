# Quatro fontes, um papel cada

Checkout `bany_mcp`, servidor `bany-mcp`, prefixo de host `mcp__banky__`. Linguagem de domínio só em `CONTEXT.md`. *Contrato* das tools na skill `$banky-mcp-tools`. AGENTS/references para runtime e segurança. Nero para knowledge operacional. A spec em `docs/spec-contrato-e-dominio.md` é o recorte grelhado (comportamento atual vs lacunas). Código de `server` + `analytics` prevalece sobre a skill se divergirem.
